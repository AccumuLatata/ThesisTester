# Program B — w8_prev30m / rvwap
# Cells: 2. Locks: MNQ touch@1min, confluence 10 (pairs), SL/TP 80/80, costs 0.5 + 1 tick/side, flatten 16:00 America/New_York.
# Bot: docs/PROGRAM_B_OPERATOR_RUNBOOK.md
# Replace dataset.path with the same 15s Quantower HE CSV used on Data.
# min_valid_confluences: 1. One required partner. No dVWAP partner.
# Regenerated by generate_program_b_yaml.py — do not hand-edit tokens.
schema_version: 1
study:
  name: progB_w8_prev30m_rvwap
  description: Program B w8_prev30m (prev30mVWAP) x rolling VWAP confirms.
  workers: 1
  confirm_above_runs: 200
  output_dir: results/studies/progB_w8_prev30m_rvwap
  dataset:
    path: 'C:\dev\ThesisTester\data\MNQ AMP Futures (Rithmic), Time - Time - 15s, 8_1_2024 120000 AM-8_7_2026 120000 AM_72578ad9-eaad-41cc-a03e-cf056050cf77.csv'
    instrument: MNQ
    format_profile: quantower_history_exporter
    source_timezone: UTC
    ingestion_mode: 15s_primary_derive_1m
  levels:
    sma_lengths:
    - 50
    - 200
    ema_lengths:
    - 9
    - 21
    sma_timeframes:
    - 1min
    - 5min
    - 30min
    ema_timeframes:
    - 1min
    - 5min
    - 30min
    vwap_windows:
    - 30min
    - 4h
    pivots_enabled: true
    pivot_timeframes:
    - 1min
    - 5min
    - 30min
    - 4h
    prev30m_vwap_enabled: true
    session_vwap_enabled: true
    single_prints_enabled: true
    apoc_enabled: true
  constants:
    direction: both
    tolerance_ticks: 10
    min_valid_confluences: 1
    naked_only: false
    naked_requirement: any
    trigger_params: {}
    entry_window: null
    backtest:
      stop_loss_ticks: 80
      take_profit_ticks: 80
      exposure_policy: single_position
      commission_per_side: 0.5
      slippage_ticks: 1.0
      flat_by_session_close: true
      session_close_time: '16:00'
      session_timezone: America/New_York
      intrabar_model: subtimeframe_conservative
    grid:
      enabled: false
    validation:
      enabled: false
    walk_forward:
      enabled: false
  factors:
    core_level:
    - prev30mVWAP
    partner_levels:
    - - VWAP_rolling_30min
    - - VWAP_rolling_4h
    confluence_mode:
    - anchor_rules
    trigger:
    - touch
    trigger_timeframe:
    - 1min
  mode_rules:
    anchor_rules:
      selected_levels: []
      anchor_level: ${core_level}
      confluence_rules:
        from_partners: required
  report:
    primary_metric: expectancy_r
    secondary_metrics:
    - profit_factor
    - max_drawdown_r
    - trade_count
    - total_r
    min_trades: 30
    multiple_testing: warn
    group_by:
    - core_level
    - partner_levels
